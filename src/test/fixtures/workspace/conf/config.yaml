# @package _global_
defaults:
  - base@_here_
  - db: mysql
  - server: apache
  - additional: other_conf

  - _self_

app_name: my_app
debug: false

output_dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}

hydra:
  searchpath:
    - pkg://additional_conf
